/**
 * Node entry for the Payload-first Ask co-pilot.
 * Implementation lives in assets/copilot.js so the browser and tests share it.
 */
"use strict";

module.exports = require("../assets/copilot");
